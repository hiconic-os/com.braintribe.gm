package com.braintribe.gm.graphfetching.api.node;

/**
 * A node representing a to-one entity property in a fetch graph (e.g. a reference to another entity).
 * Combines entity fetch node and property info.
 */
public interface EntityPropertyGraphNode extends EntityRelatedPropertyGraphNode {
	// intentionally left empty
}
